{
  "name": "gatsby-plugin-workerize-loader",
  "author": "Phuoc <phuoc317049@gmail.com>",
  "version": "1.1.0-alpha.1",
  "description": "Integrate workerize-loader with GatsbyJS",
  "license": "MIT",
  "homepage": "https://github.com/universse/gatsby-plugin-workerize-loader",
  "repository": {
    "type": "git",
    "url": "https://github.com/universse/gatsby-plugin-workerize-loader"
  },
  "keywords": [
    "gatsby",
    "gatsby-plugin",
    "worker",
    "web worker",
    "workerize",
    "workerize-loader",
    "threads",
    "webpack"
  ],
  "peerDependencies": {
    "gatsby": "^2.0.0"
  },
  "dependencies": {
    "workerize-loader": "^1.1.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.6.0",
    "@babel/core": "^7.6.0",
    "babel-preset-gatsby-package": "^0.2.4",
    "cross-env": "^5.2.1"
  },
  "scripts": {
    "build": "babel src --out-dir . --ignore **/__tests__",
    "prepare": "cross-env NODE_ENV=production npm run build",
    "watch": "babel -w src --out-dir . --ignore **/__tests__"
  }
}
